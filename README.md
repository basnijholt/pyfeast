# `pyfeast`: a Python FEAST wrapper

# Install
Install the following dependencies with
```
conda install --yes -c basnijholt feast
conda install --yes -c defaults mkl
```

## Run
```
./install_and_run.sh
```
